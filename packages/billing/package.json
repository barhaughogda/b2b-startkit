{
  "name": "@startkit/billing",
  "version": "0.1.0",
  "private": true,
  "description": "Stripe billing integration for StartKit - subscriptions, usage, and webhooks",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./stripe": "./src/stripe.ts",
    "./subscriptions": "./src/subscriptions.ts",
    "./usage": "./src/usage.ts",
    "./webhooks": "./src/webhooks.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@startkit/config": "workspace:*",
    "@startkit/database": "workspace:*",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
