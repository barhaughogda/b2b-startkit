# /run-tests-and-fix

Run the relevant test suite(s) and fix failures.

## Approach
- Start with the smallest scope (unit tests) and expand as needed.
- Fix root causes, not snapshots.
- Keep changes minimal and well-scoped.

## Output
- Commands run
- Failures fixed (summary)
- Any follow-ups
